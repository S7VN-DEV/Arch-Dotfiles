general {
    lock_cmd = pidof hyprlock || hyprlock                  # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session               # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on             # to avoid having to press a key twice to turn on the display.    
}

listener {
    timeout = 150
    on-timeout = ./scripts/bright_timeout.sh                   # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r && ddcutil setvcp 10 14   # monitor backlight restore.
}

listener {
    timeout = 200
    on-timeout = loginctl lock-session                     # lock screen when timeout has passed
}

listener {
    timeout = 500
    on-timeout = hyprctl dispatch dpms off                 # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on                   # screen on when activity is detected after timeout has fired.
}

listener {
    timeout = 600
    on-timeout = sh -c 'if [ "$(cat /sys/class/power_supply/BAT1/status)" = "Discharging" ]; then systemctl suspend; fi'
}

listener {
    timeout = 3600
    on-timeout = systemctl suspend                         # suspend pc
}
